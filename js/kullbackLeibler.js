function KullbackLeibler (entities, points) {
	this.entities = entities;
	this.points = points;
}

KullbackLeibler.prototype.calculate = function() {
	var final_results = [], current_result = 0, first, second;

	for (var x = 0; x < this.entities.length; x++) {
		
	}
};